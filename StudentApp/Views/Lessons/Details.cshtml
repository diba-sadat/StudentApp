@model StudentApp.Models.Lessons 

@{
    ViewData["Title"] = "Lesson Details";
}

<h1>Lesson Details</h1>

@if (Model != null)
{
    <div class="card">
        <div class="card-body">
            <h5 class="card-title">@Model.Name </h5>
            <dl class="row">
               </dl>
        </div>
    </div>
}
else
{
    <div class="alert alert-warning">Student not found.</div>
}


            